# Database Module

SQLAlchemy ORM models, session management, and Alembic migration configuration.

## Components

- `models.py` — SQLAlchemy table definitions
- `session.py` — Engine, SessionLocal, and `get_session()` context manager
